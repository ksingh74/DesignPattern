package CostComputationModule;

import GateModule.Ticket;

public class FourWheelerCostComputation extends CostComputation {
    @Override
    public Integer getParkingCost(Ticket ticket) {
        return super.getParkingCost(ticket);
    }
}
